import styled from "styled-components";

const TitleStyled = styled.h1`
  text-align: center;
  font-weight: bold;
  margin: 10px;
`;

export const Title = () => {
  return (
    <div>
      <TitleStyled></TitleStyled>
    </div>
  );
};
